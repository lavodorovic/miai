# Production / Streamlit Cloud runtime (keep lean for deploy).
# Tests and Playwright: pip install -r requirements-dev.txt
pandas>=2.2.0
pyarrow>=15.0.0
duckdb>=1.0.0
streamlit>=1.33.0
streamlit-echarts5>=1.0.0
